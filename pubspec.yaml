name: photo_compare
description: "Photo Compare — 團隊用前後對比拼接、本機作品庫與教學影片（MVP）"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  image: ^4.3.0
  path_provider: ^2.1.5
  permission_handler: ^11.3.1
  youtube_player_flutter: ^9.1.1
  share_plus: ^10.1.4
  intl: ^0.20.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
